namespace GRIFTools;

public static class GrodEnums
{
    /// <summary>
    /// Used to specify which dictionary(s) to use
    /// </summary>
    public enum WhichData
    {
        Both = 0,
        Base = 1,
        Overlay = 2
    }
}
